{
  "name": "jidmud",
  "version": "1.0.0",
  "description": "This is the TypeDoc README file.",
  "main": "index.js",
  "scripts": {
    "test": "mocha -r ts-node/register --file setup.spec.ts src/**/*.spec.ts",
    "coverage": "nyc --reporter=html --reporter=text npm run test",
    "make": "tsc",
    "doc": "typedoc",
    "release": "npm run coverage && npm run make && standard-version && npm run doc"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/chai": "^4.2.12",
    "@types/i18n": "^0.8.7",
    "@types/mocha": "^8.0.3",
    "@types/node": "^14.10.1",
    "chai": "^4.2.0",
    "i18n": "^0.13.2",
    "mocha": "^8.1.3",
    "nyc": "^15.1.0",
    "standard-version": "^9.0.0",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.2",
    "winston": "^3.3.3"
  }
}
